import socket
import numpy as np
from io import BytesIO

class uniqueID(object):
#  bit = 0               #Bit length of architecture
    #features = b''
    features = [None]
    name = ""
    cameraID = ""

